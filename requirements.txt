# --- Core ---
openai>=1.30.0            # 兼容 DeepSeek / GLM 的统一 SDK
pydantic>=2.7.0          # 数据模型 + schema 校验
python-dotenv>=1.0.0    # 读取 .env 配置
rich>=13.7.0            # CLI 美化 / 思考流展示

# --- RAG ---
chromadb>=0.5.0         # 本地向量库
pymupdf>=1.24.0         # PDF 解析 (import 名为 fitz)
rank-bm25>=0.2.2        # BM25 关键词召回 (hybrid 检索)

# --- Interfaces ---
fastapi>=0.111.0        # 预留 HTTP 接口
uvicorn>=0.30.0         # ASGI server (仅本地调试用)

# --- MCP ---
mcp>=1.0.0             # 官方 MCP SDK (client), 接 filesystem-mcp; 需本机 node/npx

# --- Dev ---
pytest>=8.0.0
